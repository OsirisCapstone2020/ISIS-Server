name: isis3-flask
channels:
  - usgs-astrogeology
  - conda-forge
  - defaults
dependencies:
  - python=3.6
  - isis>=4.3.0
  - pip
  - six
  - pysis>=0.6.4
  - pip:
      - flask
      - python-dotenv
      - boto3>=1.17.48
      - gunicorn
      - flask-expects-json
      - requests
